import { SubcategoriesPipe } from './subcategories.pipe';

describe('SubcategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new SubcategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
